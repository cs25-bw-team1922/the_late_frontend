import React, {useEffect, useState} from 'react'

const rooms = () => {
    const [roomData, setRooms] = useState([])

    useEffect(()=>{
        // axios call here
    })
    return (
        <>
        <h1>{room.name}</h1>
        </>
    )
}